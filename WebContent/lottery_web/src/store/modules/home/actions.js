import * as types from './mutationsTypes'
import * as apiList from 'api/member/memberApi'
import {ERR_OK} from 'api/config'

// 生成随机数
export const randomSelectNumber = ({ commit, state }) => {
  
};


